# Core cloud dependencies
flask>=3.0.0
groq>=0.5.0
requests>=2.31.0
wikipedia>=1.4.0
gunicorn>=22.0.0
psycopg2-binary

# Local (jarvis.py) — PC Remote Control (optional, Windows only)
# pyautogui>=0.9.54
# pycaw>=20220416       # For precise Windows volume control
